
import model from './model.mjs'
import routes from './routes.mjs'

export default {
    "name": "menu",
    "model": model,
    "modelName": "Menu",
    "routes": routes,

    "views": [{
        "func": (req, res, next) => {
        }
    }],
    "edits": [{
        "func": (req, res, next) => {
        }
    }],

}